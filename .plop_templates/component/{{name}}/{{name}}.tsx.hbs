import { VFC, memo } from 'react';

import styles from './{{name}}.module.scss';

type {{name}}Props = {
};

export const {{name}}: VFC<{{name}}Props> = memo((props) => {
  const {  } = props;

  return (
    <>
    </>
  );
});
